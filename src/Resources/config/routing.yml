thepixeldeveloper_sitemap_bundle_controller:
    path: /{sitemap}.xml
    methods: [GET]
    defaults: { _controller: 'SitemapBundle:Sitemap:sitemap' }
    requirements:
        sitemap: 'sitemap|urlset-\d+'
